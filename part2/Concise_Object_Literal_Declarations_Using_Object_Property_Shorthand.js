//const createPerson = (name, age, gender) => {
 // return {
   // name: name,
    //age: age,
    //gender: gender
  //};
  // Only change code above this line
//};
//above is same as below
const createPerson = (name, age, gender) => ({
  name, age, gender
});
