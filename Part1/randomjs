//random fraction
function randomFraction() {
  var result= 0;
  while (result=== 0){
    result= Math.random();
  }
  return result;
  
//random whole numbers

function randomWholeNum() {
  return Math.floor(Math.random() * 10);
}
